language: rust
rust:
  - stable
  - nightly
matrix:
  allow_failures:
    - rust: nightly
env:
  - RUST_BACKTRACE=1
before_script:
  - rustup component add rustfmt clippy 
script:
  - cargo fmt -- --check
  - cargo clippy -- -D clippy::pedantic
  - cargo test --all
